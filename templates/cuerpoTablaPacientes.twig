{#
Datos esperados
array('pacientes' => array( Paciente ),
      'permisos' => array('index', 'new', 'update', 'destroy', 'show' )
      );
#}

{% for paciente in pacientes %}
  <tr>
    <td class="textcenter" >{{ paciente.getNombre |capitalize }}</td>
    <td class="textcenter" >{{ paciente.getApellido |capitalize }}</td>
    <td class="textcenter" >{{ paciente.getNombreTipoDocumento }}</td>
    <td class="textcenter" >{{ paciente.getNumero }}</td>
    <td class="textcenter" >{{ paciente.getNro_historia_clinica }}</td>
    <td class="textcenter" >{{ paciente.getNombreObraSocial }}</td>
    <td id="{{ paciente.getId }}">
      {% for permiso in permisos %}
        {% include "moduloPaciente/boton_#{permiso}.twig" ignore missing %}
      {% endfor %}
    </td>
  </tr> 
  {% else %}
    <td colspan="7" class="textcenter" >No hay pacientes para mostrar</td>
{% endfor %}
