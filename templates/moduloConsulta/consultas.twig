{% extends 'layoutLogueado.twig' %}

{#
Datos esperados
array('pacientes' => array( Paciente ),
      'tiposDocumentos' => array( TipoDocumento ),
      'permisos' => array('index', 'new', 'update', 'destroy', 'show' )
      );
#}

{% block titulo %} Consultas {% endblock %}

{% block cuerpo %}
  <div class="container my-3">
    <ul class="nav nav-tabs" id="menuTabs" role="tablist">
      {% for permiso in permisos %}
        {% include "moduloConsulta/tab_#{permiso}.twig" ignore missing %}
      {% endfor %}
    </ul>
    <div class="tab-content" id="myTabContent">
      {% for permiso in permisos %}
        {% include "moduloConsulta/cuerpo_#{permiso}.twig" ignore missing %}
      {% endfor %}
    </div>
  </div>

  <!-- Modal Mensaje Confirmacion -->
  <div class="modal fade" id="mensajeConfirmacion" tabindex="-1" role="dialog" aria-labelledby="tituloMensaje" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title ml-auto" id="tituloMensaje">titulo</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Boton para cerrar">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div id="cuerpoMensaje" class="modal-body">

        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-outline-secondary" data-dismiss="modal">Cerrar</button>
          <button id="botonMensaje" type="button" class="btn btn-outline-success" data-dismiss="modal">Confirmar</button>
        </div>
      </div>
    </div>
  </div>
{% endblock %}

{% block scripts %}
  <script src="http://www.openlayers.org/api/OpenLayers.js"></script>
  <script src="templates/js/consultas.js"></script>
  <script src="templates/js/consultas-BusquedaPaciente.js"></script>
{% endblock %}
